=== docker build --no-cache output ===
(full build log — see terminal scrollback; build completed successfully, [+] Building 7.5s (13/13) FINISHED, all layers built from scratch, no cache reused)

=== Image size (unambiguous, via docker inspect) ===
Size in bytes: 44929692
Size in MB: 42.8 MB
Requirement: under 100MB. Result: PASS (42.8MB).

Note: 'docker image ls' on this Docker version reports 181MB in its SIZE column,
which is a virtual/disk-accounting figure that includes shared base-layer bytes
attributed per-image, not the image's actual logical size. The authoritative
figure is docker inspect's .Size field (42.8MB), used above.

=== whoami ===
kijani

=== which tsc (must be empty - confirms no TypeScript compiler in image) ===
(exit code: 1 — nonzero confirms tsc not found)

=== HEALTHCHECK behavior ===
Container started, health status was 'starting' at 2s (within start-period),
transitioned to 'healthy' by 35s (after the 30s start-period elapsed).
Confirmed via: docker inspect --format='{{.State.Health.Status}}' -> healthy
